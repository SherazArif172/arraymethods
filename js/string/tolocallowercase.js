const str = "Hello Pakistan , Where are you";

console.log(str.toLocaleLowerCase());

// it coverts all the string to lower case using the current language of browser
// same as the toLowerCase() method but it uses the current language of browser to convert the string to lower case

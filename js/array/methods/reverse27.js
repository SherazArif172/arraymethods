const arr = [1, 2, "dfsf", { name: "dsfsd" }, 2, 3, 4, 4, 5, 6, 6];

console.log(arr.reverse());
console.log(arr);

// overwrites the origional array
// reverse the order of the elements of the a targted array

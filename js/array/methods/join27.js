const arr = ["pakistan", "zindabad"];

console.log(arr.join());
console.log(arr.join(" "));
console.log(arr.join("-"));

// does not change the origional array
// returns an array with he given value in join seprated values by this

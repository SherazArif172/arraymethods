const arr = [1, 2, 2, 3, 4, 4, 5, 6, 6];

const arr2 = arr.valueOf();

console.log(arr2);
console.log(arr);

// method returns the array itself
// does not modifies thee origionl array
// modifies the origional array

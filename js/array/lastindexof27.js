const arr = [1, 2, 2, 3, 4, 4, 5, 6, 6];

console.log(arr.lastIndexOf(6));

// if we have multiple same values in a array , it will return the last index of the matched value
// does not modifies the origional array
